#Add digits of a number until it's just one digit

num = 9875

while num >= 10:
    sum_of_digits = 0
    temp = num
    while temp > 0:
        digit = temp % 10
        sum_of_digits = sum_of_digits + digit
        temp = temp // 10
    num = sum_of_digits

print("Final single digit:", num)
